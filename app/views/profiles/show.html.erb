<div class="page-header">
  <h1><%= @user.full_name %></h1>
</div>

<% if @statuses %>
  <% @statuses.each do |s| %>
    <div class="well">
      <%= s.content %>
      <hr />
      <%= link_to time_ago_in_words(s.created_at), status_path(s) %> ago
    </div>
  <% end %>
<% end %>
